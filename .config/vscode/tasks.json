{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
          "label": "python-formatting",
          "type": "shell",
          "command": "autoflake -i --remove-all-unused-imports ${file} && isort ${file} && black ${file}",
          "presentation": {
            "reveal": "silent",
          }
        }
    ]
}