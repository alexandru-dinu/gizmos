#!/bin/bash

case "$1" in
--host)
    echo "$(whoami) @ $(hostname)"
    ;;
--ram)
    free -h | awk '(NR==2){print $7}' | tr -d 'i'
    ;;
--storage)
    df / -h --output=avail | awk '(NR==2){printf "%s", $1}'
    ;;
--load)
    cut -d' ' -f1-3 /proc/loadavg
    ;;
*) ;;

esac
