#!/usr/bin/env bash

# open "search term" in Youtube Music Brave App

set -euo pipefail

fzf \
    | tr ' ' '+' \
    | awk '{print "https://music.youtube.com/search?q=" $0}' \
    | xargs -I "{}" "/Applications/Brave Browser.app/Contents/MacOS/Brave Browser" --app="{}"
