#!/bin/bash

brightnessctl set "$1"

level=$(brightnessctl | rg -o "(\d+)%")

dunstify \
    -a "$(basename $0)" \
    -r "999001" \
    -u low \
    -t 1000 \
    -h int:value:"$level" "Brightness: ${level}"
